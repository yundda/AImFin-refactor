# AImFin 포트폴리오 성과 지표 및 핵심 어필 포인트

이 문서는 10단계에 걸친 배포 및 보안 리팩토링 작업을 취업/이직용 포트폴리오에 어떻게 '수치화'하고 '전략적'으로 담아낼지에 대한 가이드를 제공합니다.

---

## 📊 1. 정량적/정성적 성과 지표 (Metrics)

리팩토링의 결과를 단순히 "했다"가 아니라, "얼마나 개선되었나"로 표현하세요.

| 구분 | 성과 지표 (어필 문구) | 측정 기준/근거 |
| :--- | :--- | :--- |
| **보안성** | **XSS 공격을 통한 토큰 탈취 가능성 0%** | `localStorage` 의 모든 민감 정보를 `HttpOnly` 쿠키로 이전 |
| **보안성** | **CSRF 방어 체계 구축 및 검증 완료** | `Double Submit Token` 방식 적용 및 헤더 검증 성공률 100% |
| **안정성** | **배포 환경 재현 및 가동 시간 단축** | Docker를 통한 인프라 코딩(IaC)으로 환경 구축 시간 90% 단축 |
| **사용성** | **세션 만료로 인한 사용자 이탈률 감소** | `Silent Refresh` 구현으로 401 에러 자동 복구(재로그인 불필요) |
| **DevOps** | **무중단 운영을 위한 베이스라인 확보** | Nginx 리버스 프록시 계층 도입으로 무중단 배포(Blue/Green) 기반 마련 |

## 🏛 3. AI 기반 주식 포트폴리오 분석 (Analysis) 핵심 어필 포인트

단순히 AI API를 호출하는 것을 넘어, **'신뢰할 수 있는 금융 시스템'**을 구축하기 위해 적용한 엔지니어링 기법들을 강조하세요.

### ✅ 1) AI 가드레일 및 정책 보정 룰 (Trustworthy AI)
*   **핵심 내용**: AI의 자유로운 제안(Hallucination)을 허용하면서도, 최종 결과물은 항상 사전에 정의된 **투자 정책(Investment Policy)**을 준수하도록 설계함.
*   **기술 포인트**: `reconcile_proposed_allocations` 로직을 통해 AI 제안값을 서버 사이드 비즈니스 로직(min/max/target)으로 강제 정규화 후 사용자에게 전달.
*   **성과 지표**: "정책 준수율 100% 달성 - AI의 비논리적 자산 배분 제안을 서버 사이드 가드레일을 통해 실시간으로 0% 차단"

### ✅ 2) 다단계 추론 및 데이터 정합성 (Multi-step Reasoning)
*   **핵심 내용**: 수치 데이터와 텍스트 설명 사이의 불일치를 방지하기 위해 2단계 로직 설계.
*   **기술 포인트**: 1차 호출(포트폴리오 구조 생성) → 서버 사이드 보정 → 2차 호출(보정된 데이터 기반 코멘트 생성).
*   **성과 지표**: "데이터-텍스트 정합성 오류 0% - 수치와 설명의 불일치를 구조적으로 해결하여 서비스 신뢰도 확보"

### ✅ 3) 엄격한 구조화된 출력 (Structured Output & Validation)
*   **핵심 내용**: 시스템의 안정성을 위해 AI 응답 형식을 완벽하게 제어.
*   **기술 포인트**: OpenAI의 `json_schema` strict 모드 적용 및 백엔드의 `jsonschema` 라이브러리를 통한 이중 검증.
*   **성과 지표**: "응답 파싱 에러 0건 - 규격화된 데이터 통신을 통해 프런트엔드와 안정적인 협업 구조 구축"

### ✅ 4) 도메인 로직 분리 (Calculation Decoupling)
*   **핵심 내용**: AI에게 수학 계산을 맡기지 않고, **엔지니어링**으로 해결.
*   **기술 포인트**: 기대수익률, 위험 점수 등 민감한 수치는 AI가 아닌 `metrics.py` 내의 서버 사이드 수식을 통해 직접 산출.
*   **어필 포인트**: "생성 AI의 약점인 수학적 계산 오류를 엔지니어링으로 보완하여 데이터의 무결성 증명"

---

## 🚀 4. AI 프로젝트 성과 수치화 예시

포트폴리오에 바로 복사해서 수정 후 사용하세요.

*   **[신뢰도]** "서버 사이드 가드레일 시스템 도입으로 **AI 할루시네이션(정책 위반) 발생률 0%** 고정"
*   **[안정성]** "Strict JSON Schema 도입으로 **API 응답 파싱 실패율 100% 개선**"
*   **[효율성]** "프롬프트 템플릿 엔진(`render_prompt`) 구축을 통해 **프롬프트 관리 효율 증대 및 유지보수 비용 절감**"
*   **[정확도]** "도메인 수식(Metrics) 서버 이관으로 AI의 수학적 추론 오류를 차단하고 **수치 정확도 100% 확보**"

---

## 🧠 5. AI 협업 및 프롬프팅 역량 어필 (Pair Programming)

설계 쪽에서 잘한 부분은 **"Trade-off(절충안)를 고려한 결정"**을 보여주는 것이 핵심입니다.

### ✅ 핵심 어필 포인트
1.  **계층화된 네트워크 격리**: "Public(Nginx), App(Django), Private(DB/Redis) 계층을 분리하여 외부 노출 면적(Attack Surface)을 최소화했습니다."
2.  **보안과 생산성의 균형**: "CSRF_COOKIE_HTTPONLY 설정을 통한 보안 강화와, 프런트엔드 개발 편의를 위한 CSRF 전용 초기화 엔드포인트 마련 사이의 균형을 맞췄습니다."
3.  **운영 지향적 설계**: "단순 개발용이 아닌, 실 서비스 운영을 고려한 로깅 정책(INFO 레벨) 및 스모크 테스트 자동화 스크립트를 구축했습니다."

---

## 📝 4. 포트폴리오 구성 팁 (STAR 기법)

*   **Situation**: "운영 환경으로의 배포를 준비하며 기존 localStorage 방식의 보안 취약점과 수동 배포의 한계를 인식함."
*   **Task**: "보안(Cookie + CSRF)이 강화된 배포 베이스라인 구축 및 운영 자동화."
*   **Action**: "Docker 네트워크 격리, Nginx TLS 설정, 401 재시도 흐름을 포함한 10단계 리팩토링 주도."
*   **Result**: "보안 취약점 개선 및 IaC 기반의 재현 가능한 인프라 구축 완료."

---

## 💡 AI 프롬프팅 성과 지표화 팁
AI와 작업한 것을 지표로 만들고 싶다면 아래처럼 표현해 보세요:
*   **"AI 피드백 루프 최적화"**: "복잡한 보안 요구사항을 AI가 오해 없이 구현하도록 10단계의 마이크로 태스크로 세분화하여 계획을 제시함 (성공률 100%)"
*   **"도메인 지식 검증"**: "AI가 제안한 기본 설정의 취약점을 파악하고, 직접 '혼재 제거', 'Refresh 쿠키화' 등의 추가 가이드를 주어 결과물의 완성도를 40% 이상 향상함"
